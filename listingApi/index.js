/**
 * simplyrets API class to query properties.
 */
var btoa = require('btoa');
var fetch = require('cross-fetch')

const simplyRETS = require('./simplyRETS')

//TODO : this can be a wrapper class fetching listing from multiple sources
module.exports = {
    simplyRETS
}